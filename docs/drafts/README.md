# 草稿

这个目录被 `srcExclude` 排除在构建之外：里面的文件**不会生成页面**，没有网址，不进导航、列表和搜索。

写好了要发布，用写作台的「移动/改名」把它挪到正式位置，例如：

```
drafts/rag-chunking.md  →  posts/rag-chunking.md
```

⚠️ 仓库是 public 的。草稿在站点上看不到，但在 GitHub 仓库里任何人都能读。真正不想被看到的内容别放这里。
